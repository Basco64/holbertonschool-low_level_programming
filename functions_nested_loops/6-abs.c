#include "main.h"

/**
 * _abs - Function for calculate absolute value of an integer
 *
 * @num: the input
 *
 * Return: the absolute value of the input
*/

int _abs(int num)
{
	if (num >= 0)
		return (num);
	else
		return (-num);
}
