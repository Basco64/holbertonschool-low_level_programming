
#include "main.h"

/**
 * print_number -  Prints an integer
 *
 * @n: the integer
 *
 * Return: the integer
*/

void print_number(int n)
{
	int u, d;

	if (n < 0)
		_putchar('-');

	d = 100000

	while (i > 0)
	{
		if (i <= u || i == 1)
			_putchar(u / i % 10 + '0');
		i /= 10;
	}
}
